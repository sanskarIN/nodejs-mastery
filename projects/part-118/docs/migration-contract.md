# Migration Contract

Every ownership transfer increments a fencing epoch. Old workers cannot commit after a new owner is active. Migration evidence records source, target, epoch, readiness, and recovery result.
