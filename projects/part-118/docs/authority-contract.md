# Authority Contract

Clients request actions; they do not write authoritative world state. The server validates commands, orders them deterministically, applies domain rules, and publishes authoritative results.
