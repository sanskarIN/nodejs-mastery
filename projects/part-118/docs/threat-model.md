# Threat Model

Assume clients can be modified, messages can be duplicated or delayed, clocks can be wrong, sessions can reconnect through stale routes, and individual workers can fail. Protect server authority, epoch fencing, bounded resources, secrets, and durable recovery evidence.
