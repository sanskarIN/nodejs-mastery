# Protocol Contract

Every network message has a schema version, session identity, player identity, sequence, authoritative epoch, size limit, authentication context, and explicit rejection behavior.
