import fs from 'node:fs'; const required=['package.json','README.md','src/kernel.js','src/demo.js','test/kernel.test.js']; const missing=required.filter(p=>!fs.existsSync(new URL('../'+p,import.meta.url))); if(missing.length)throw new Error('missing '+missing); console.log('verify ok');